# Docker Environment Variables
# Copy this file to .env.docker and adjust values as needed

# Database Configuration
DB_NAME=poker_tracker
DB_USERNAME=postgres
DB_PASSWORD=postgres123
DB_PORT=5432

# pgAdmin Configuration
PGADMIN_EMAIL=admin@poker-tracker.com
PGADMIN_PASSWORD=admin123
PGADMIN_PORT=5050

# API Configuration (for reference)
# These should match your packages/api/.env file
API_PORT=4000
API_HOST=localhost

# Web Configuration (for reference)
# These should match your packages/web/.env.local file
WEB_PORT=3000
WEB_HOST=localhost
